@page "/game"
@inherits GameComponent

<div class="container">
    <h1>@RoomCode</h1>
    <ul id="discussion">
        @foreach (var message in messages)
        {
            <li>@message</li>
        }
    </ul>
</div>